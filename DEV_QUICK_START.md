# 开发服务器快速启动

## 🚀 快速命令

```bash
# 启动服务器（后台运行）
./dev.sh start

# 停止服务器
./dev.sh stop

# 重启服务器
./dev.sh restart

# 查看状态
./dev.sh status

# 查看日志
./dev.sh logs
```

## 📋 常用场景

### 首次启动

```bash
cd /data/ai-stock-web
./dev.sh start
```

访问: `http://121.196.147.222:3333`

### 代码更新

```bash
git pull
./dev.sh restart
```

### 查看运行状态

```bash
./dev.sh status
```

### 排查问题

```bash
./dev.sh logs
```

## 📁 文件说明

- `dev.pid` - 进程 ID 文件
- `dev.log` - 日志文件

## 🔧 故障排查

### 端口被占用

```bash
# 查看占用进程
lsof -i :3333

# 终止进程
kill -9 <PID>

# 重启服务器
./dev.sh restart
```

### 服务器无响应

```bash
# 强制停止
pkill -9 -f vite
rm -f dev.pid

# 重新启动
./dev.sh start
```

### 查看完整日志

```bash
cat dev.log
```

## ⚠️ 注意事项

1. **开发环境端口**: 3333
2. **生产环境端口**: 80 (Nginx)
3. **不要在生产环境使用 dev.sh**
4. **生产部署使用**: `sudo ./deploy-local.sh`

## 📚 详细文档

查看完整文档: `docs/dev-server-guide.md`
