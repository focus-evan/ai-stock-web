/**
 * System management fake routes - DISABLED
 * Real backend APIs are at /api/system/*
 * Keep this file as empty to avoid conflicts with real API.
 */
import { defineFakeRoute } from "vite-plugin-fake-server/client";

export default defineFakeRoute([]);
