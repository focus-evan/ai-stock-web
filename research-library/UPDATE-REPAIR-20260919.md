# 内容中心更新链路修复

## 问题及修复

- 线上日更内容仍是9月7日的静态快照，本地晨报、全球科技、V231、四战法已有9月18/19日版本。现在提供独立增量内容发布器，生成完成后自动合并、验证、上传并切换，整站发布保留独立内容指针。
- 大盘情绪任务还写入迁移前目录。已将最新报告、每日复盘及预测台账补回 `D:\Evan\html\a-share-macro-radar`，旧根目录保留；覆盖前备份位于 `D:\Evan\html\_tools\research-repair-backup-20260919`。
- 日期读取优先使用报告对应JSON，区分生成时间与行情日期。V231不再把窗口起点当作报告日期，四战法有明确时间。
- 卡片与阅读页动态显示更新状态、等待新一期或暂停状态，包含工作日/周末和每天两次更新的边界；已打开的页面每分钟提示新一期。
- 财报公告监控继续保持用户要求的暂停状态。多来源合并保留33个模块，历史正文不重写；本次同步后2262篇HTML、1805份资料。

## 已完成验证

- Node定向回归11项：日期、周末/星期一、午间刷新、暂停、历史页、跨来源保留、路径/长文件名。
- Linux发布事务测试4项：成功保留旧版字节、HTTP失败恢复旧版、并发版本冲突拒绝、路径越界拒绝。
- 全内容链接与来源正文一致性校验通过；TypeScript检查通过，生产构建通过。
- 浏览器确认：目录保留33模块，日报日期为9月18/19，持仓阅读页显示9月18日报告与9月17行情；暂停提示正确。

## 发布边界及维护

生产原前端检出存在既有锁文件修改；未运行其原地构建脚本。修复在 `origin/main` 的独立工作区验证，发布只修改 `/research-library/` 静态内容及专用内容发布目录，不变更业务API、数据库、Nginx配置或端口80。

运行 `node scripts/publish-research-library.mjs`，结果仅可能为 published / unchanged / busy 或失败退出。最终线上版本和回滚目录以服务器 `/data/research-library-publisher/last-publish.json` 及本地 `research-library/publish-state/last-publish.json` 为准；没有公网校验成功不能宣称已上线。

上传与本地临时包在成功后清除；旧内容发布和首次迁移前的目录均保留可恢复。发布工作区被自动化引用，应长期保留。
