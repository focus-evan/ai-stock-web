# 内容中心迁移与验证

完成日期：2026-09-07。工作分支：`codex/research-library`，基于 `main` 提交 `38712ce`。

## 已完成

- 工作台首页增加「研究与知识库」大入口，菜单新增同名入口；路径 `/research-center`。
- 独立内容中心路径 `/research-library/index.html`，支持分类、专题、全部报告、资料附件、关键词检索、排序、分页与浏览器返回恢复筛选状态。
- 23 个专题、767 个报告页面、614 份资料附件，共同步 1,399 个内容文件，原文件合计约 211.87 MiB。
- 原目录 `D:\Evan\html` 保留，自动化继续向该目录生成报告。启动开发服务和构建时，在来源存在的机器上自动同步；也可执行 `corepack pnpm research:sync`。
- 4 个浏览器扩展内部 HTML、浏览器缓存/个人配置、数据库和运行中间文件未进入站点。文档明确引用的 6 个 SH/CONF 资料作为 TXT 副本收录，可读可下载，不执行。
- 修复 1,251 处归档页面和历史目录副本的相对路径；保留专题与历史层级。
- 每个报告加入统一返回导航、专题入口、字号调节、回顶部；小屏网格改为单列，宽表格在局部区域横向滚动。
- 静态界面资源使用内容哈希查询参数，避免部署后长期缓存继续显示旧的阅读样式。

## 验证证据

| 项目 | 结果 |
| --- | --- |
| 全量本地链接与资源检查 | 4,712 处检查，0 缺失 |
| 报告正文比较 | 767 页与原始文档的正文文本一致 |
| 原文件 SHA256 | 全部同步来源与清单匹配，0 改动 |
| 数据与附件 | 614 份均有实际文件 |
| 新增 Node 迁移/检索检查 | 5 项通过 |
| 新增前后端菜单模式路由测试 | 3 项通过 |
| 原项目自动化测试 | 30 项通过 |
| TypeScript | 无错误 |
| 生产构建 | 通过，包含内容中心与内容快照 |
| 缺少来源目录的构建准备路径 | 使用仓库快照，验证通过 |
| 桌面视口 1366 × 900 | 3 列专题卡片，无整页横向溢出 |
| 手机视口 390 × 844、320 × 740 | 内容中心无整页横向溢出 |
| 手机阅读长审计报告 | 20 个宽表格独立滚动；正文放大到 19px 有效 |
| 手机阅读 S老师方法论长文 | 无整页横向溢出，3 个表格均有滚动容器 |
| 搜索、分类与分页 | 实测搜索“半导体”、叠加“个股与持仓”、全部报告第 2 页均正常 |
| 工作台登录衔接 | 未登录访问 `/research-center` 跳转 `/login?redirect=/research-center` |

原项目测试在全部断言通过后仍有 Vite 退出等待超时提示，退出码为 0；构建仍有原有大包提示。浏览器实测覆盖独立内容中心与代表性报告；未用生产账号登录进行写入或执行任何业务操作。

## 内容更新与发布边界

开发服务已在本机提供预览：`http://127.0.0.1:4371/research-library/index.html`。这是本机预览地址，不是新的线上地址。

本记录对应提交前的本地整合与验证；远程提交和生产验收应以发布任务的独立记录为准。静态文件并不由 React 登录页面保护；未来若需限制外部访问，应在服务器层对 `/research-library/` 设置访问控制。工作台原有交易路由、登录守卫和后端明确返回的同名菜单权限保持原语义。

同步后的详细文件清单、路径修复清单与排除项见 [sync-report.json](sync-report.json)。使用和维护方式见 [README.md](README.md)。
